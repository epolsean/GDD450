using UnityEngine;
using System.Collections;

/* this script will contain all methods and variables used when moving 
 * pieces and attacking during a turn, it will call A* to start a piece 
 * moving and also test the distance to the target tile between the 
 * current tile to check if it is a possible legal move. 
 */

public class OnTurnActions : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
