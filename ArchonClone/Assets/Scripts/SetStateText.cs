using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class SetStateText : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //this.GetComponent<Text>().text = TurnStateMachine.state.ToString();
	}
}
